{
  "name": "livestream_app",
  "version": "1.0.0",
  "description": "To get started with the Livestream Overlays app, follow these simple steps:",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "pip install -r requirements.txt && cd frontend && npm i && npm run build && cd .. && python3 index.py"
  },
  "author": "",
  "license": "ISC"
}
